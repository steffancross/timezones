'use client';

/**
 * Stub during section E build. F4 replaces this with the real DST proximity
 * banner. Keeping the import path stable so E9's composition doesn't need
 * editing when F4 lands.
 */
export function DSTBanner() {
  return null;
}
